CREATE TABLE IF NOT EXISTS commodity.oil(
    year integer,
    month integer,
    originName text,
    originTypeName text,
    destinationName text,
    destinationTypeName text,
    gradeName text,
    quantity integer
)